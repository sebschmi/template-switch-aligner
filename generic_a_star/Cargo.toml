[package]
name = "generic_a_star"
version = "0.5.0"
edition = "2021"
rust-version.workspace = true

[features]
serde = ["dep:serde"]

[dependencies]
binary-heap-plus = "0.5.0"
deterministic_default_hasher = { version = "0.5.0", path = "../deterministic_default_hasher" }
num-traits.workspace = true
serde = { workspace = true, features = ["derive"], optional = true }
